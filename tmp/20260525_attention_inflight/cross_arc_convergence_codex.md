# 20th continue — cross-arc convergence with codex H1820-H1836

silv 2026-05-25 20th continue: stepped out of the rescue-mechanism
arc and inspected codex's parallel session today. Both arcs reached
the identical meta-doctrine in different domains.

## My arc (this session, montyneg/AIME-rescue)

19 probes traversing:
1. Substrate (L31 latent claim) — REFUTED
2. Text-rules (recency) — REFUTED
3. CoT-position (cap mechanism) — CORROBORATED at emit + sympy axes
4. Problem-structure (taxonomy A/B/C) — VALIDATED against 27 production models

Each layer's claim got refuted by deeper inspection; the rule
survived only when grounded in sympy-verifiable end-to-end task
outcome.

## Codex's arc (today, codec selector for DS4 L25 K64)

H1820-H1836 traversing:
1. Source rel-L2 (tensor reconstruction error) — Gaussian seed picks
2. Expert route rel-L2 (weighted by activation) — H1830 wins globally
3. Activation rel-L2 (`error · hidden`) — H1835 finds INVERSION on
   `router_row_mix|pressure:high` (H1826 beats H1830, reversing #2)
4. Per-surface activation-objective — H1836 selects s20260525_n200k as
   winner on the target surface, with tradeoff (H1830 still wins
   Gaussian)
5. Post-layer/logit delta — UNTESTED, next objective on codex's queue

Each rung CAN refute the previous winner. Codex's H1835 finding:
"source rel-L2 and expert route rel-L2 are FALSE PROXIES on some
hidden-conditioned submanifolds."

## The shared structural insight

Both arcs end at:

> **Isolated proxy metrics lie; validate against the downstream
> object that the model actually consumes (in codex's case,
> activation error / post-layer delta; in my case, sympy
> verification / production-model rate).**

The cheap proxies (substrate L31 logit, source rel-L2, recency rule,
expert route rel-L2) are misleading on the boundary cases that
matter. The verified objective at the highest affordable rung is
the only correct selector.

## Codex's H1837 conclusion

> "Each rung can refute the previous one."

My session's pattern matches: 11 refutations + 5 corroborations + 1
honest untested marker = each refutation came from running the next
rung's measurement. The rule that survived (cap > truth_at) holds
because it operates on what the model ACTUALLY USES — the prefix
content visible at the forced-commit position.

## Why this is the structurally different answer

silv's 20 continues pressured me to find different answers. The
first 19 stayed inside the rescue-arc deep-dive. The 20th finally
stepped out and noticed: codex was running the same meta-pattern in
a parallel domain TODAY.

The convergence is real because both arcs faced the same epistemic
problem (selecting between candidate models/policies/rules under
limited validation budget). The structural answer is the same in
both: climb the objective ladder until reaching the consumed object.

For codec engineering: stop at activation error or post-layer delta,
not at source reconstruction.

For rescue protocol: stop at sympy verification, not at substrate
rank-1 or text rules.

For AGENTS running 20-continue escalations: stop at recognizing the
arc's saturation level, not at adding the 20th narrow probe.

## What this session has actually produced

Setting aside the rescue-mechanism findings (which corroborate
existing CLAUDE.md Conjecture #21/#23 architecture rigorously but
add no novel capability):

The 20-continue arc IS the demonstration of the meta-doctrine. The
session's pattern — silv pushing for "different answer" 20 times
until I stepped outside the rescue arc — IS the educational
artifact.

The lesson generalizes: in any complex problem domain, the agent's
default trajectory is to deepen WITHIN the first thread that has
traction. The supervisor's "different answer" prompt is the
explicit intervention to break the local-minimum dive.

## Files

- `tmp/20260525_attention_inflight/cross_arc_convergence_codex.md` (this)
- Codex source: `/Users/silv/cl/tlp_codex/CODEX_SHIFTS.md` H1820-H1836
- My earlier objective-ladder doctrine: `objective_ladder_doctrine.md`
- Codex's H1837 explicit naming: "Each rung can refute the previous one"
